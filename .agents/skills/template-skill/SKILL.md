---
name: template-skill
description: A base template for creating new skills in this repository.
---

# 🛠️ Template Skill

This is a base template designed to be copied when creating new skills.

## 📐 Structure

Each skill should follow this structure:

- `SKILL.md`: Documentation and metadata.
- `scripts/`: Implementation logic (Python, Bash, JS, etc.).
- `resources/`: Static assets, templates, or data files.

## 🚀 Usage

1. Copy this directory to a new folder in `.agents/skills/`.
2. Update the `name` and `description` in `SKILL.md`.
3. Implement your logic in the `scripts/` directory.
